package task4;

import java.math.BigDecimal;
import java.util.List;

public class MyaUtils4 {
	 public List<Employee> largestEmployees(List<Employee> workers) {
		 for (Employee e : workers) {
			 BigDecimal maxPay = null;
			 if (e.getPayment() > max) {
				 max = e.getPayment();
			 }
			 int maxExp = 0;
			 if (e.getExperience() > maxExp) {
				 maxExp = e.getExperience();
			 }
		 }
		return workers;  
	    }
	 public Employee getMaxPayment(List<Employee> workers) {
		 int max = 0;
		 for (Employee e : workers) {
			 BigDecimal maxPay = null;
			 if (e.getPayment() > max) {
				 
			 }
		return e;
	 }
}
